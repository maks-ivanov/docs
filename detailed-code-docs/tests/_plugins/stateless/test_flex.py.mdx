---
title: "test_flex.py"
---

## High-level description

The target file `tests/_plugins/stateless/test_flex.py` contains unit tests for the `vstack` and `hstack` functions from the `marimo._plugins.stateless.flex` module. These functions are used to create HTML structures that stack items either vertically or horizontally using CSS flexbox properties. The tests verify that the generated HTML matches the expected output for different configurations of alignment, justification, and spacing.

## Code Structure

The main symbols in the code are the test functions `test_vstack` and `test_hstack`. These functions call the `vstack` and `hstack` functions, respectively, with various parameters to test their behavior. The expected HTML output is then asserted against the actual output to ensure correctness.

## References

- `vstack`: A function from `marimo._plugins.stateless.flex` that stacks items vertically.
- `hstack`: A function from `marimo._plugins.stateless.flex` that stacks items horizontally.

## Symbols

### `test_vstack`
#### Description
Tests the `vstack` function to ensure it correctly generates HTML for vertically stacked items with specified alignment, justification, and spacing.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| None | None | This function does not take any parameters. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| None | None | This function does not return any value. |

#### Internal Logic
- Calls `vstack` with a list of items and various parameters.
- Asserts that the generated HTML matches the expected output.

### `test_hstack`
#### Description
Tests the `hstack` function to ensure it correctly generates HTML for horizontally stacked items with specified alignment, justification, and spacing.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| None | None | This function does not take any parameters. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| None | None | This function does not return any value. |

#### Internal Logic
- Calls `hstack` with a list of items and various parameters.
- Asserts that the generated HTML matches the expected output.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `marimo._plugins.stateless.flex` | Provides the `vstack` and `hstack` functions used in the tests. |

## Error Handling

The test functions use assertions to handle errors. If the generated HTML does not match the expected output, an assertion error will be raised, indicating a failure in the test.

## Logging

There is no explicit logging in the test code. The test framework used to run these tests would typically handle logging of test results.

## TODOs

There are no TODOs or notes left in the target file.