---
title: "Overview"
---

```diff
--- a/frontends/chat/src/components/contexts/UserContext.tsx
+++ b/frontends/chat/src/components/contexts/UserContext.tsx
@@ -35,6 +35,7 @@
   datasetsAndUsages: Accessor&lt;DatasetAndUsageDTO[]&gt; | null;
   login: () =&gt; void;
   logout: () =&gt; void;
+  refetchDatasets: () =&gt; void;
 }
 
 export const UserContext = createContext&lt;UserStore&gt;({
@@ -48,6 +49,7 @@
   datasetsAndUsages: null,
   login: () =&gt; {},
   logout: () =&gt; {},
+  refetchDatasets: () =&gt; {},
 });
 
 export const UserContextWrapper = (props: UserStoreContextProps) =&gt; {
@@ -112,6 +114,10 @@
       });
   };
 
+  const refetchDatasets = () =&gt; {
+    setSelectedOrganization(selectedOrganization());
+  };
+
   createEffect(() =&gt; {
     const api_host: string = import.meta.env.VITE_API_HOST as string;
     const organization = selectedOrganization();
@@ -207,6 +213,7 @@
     datasetsAndUsages: datasetsAndUsages,
     login: login,
     logout: () =&gt; {},
+    refetchDatasets: refetchDatasets,
   };
 
   return (

```