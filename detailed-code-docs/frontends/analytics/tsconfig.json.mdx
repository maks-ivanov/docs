---
title: "tsconfig.json"
---

## High-level description
This file, `tsconfig.json`, configures the TypeScript compiler for the `analytics` frontend. It extends a base configuration from `config/tsconfig` and disables the `noUnusedLocals` check.

## Table of contents
- `extends`
- `compilerOptions`
- `include`

## References
- `config/tsconfig`: This file extends a base TypeScript configuration located at this path.

## Symbols
### `extends`
#### Description
Specifies the path to a base configuration file to inherit settings from. This promotes reusability and consistency across different parts of the codebase.

#### Inputs
N/A - This is a configuration property, not a function.

#### Outputs
N/A - This is a configuration property, not a function.

#### Internal Logic
N/A - This property directly points to another configuration file.

### `compilerOptions`
#### Description
Contains compiler flags to customize the TypeScript compilation process.

#### Inputs
N/A - This is a configuration property, not a function.

#### Outputs
N/A - This is a configuration property, not a function.

#### Internal Logic
- `noUnusedLocals`: This flag, set to `false`, disables the check for unused local variables within TypeScript files.

### `include`
#### Description
Specifies the files and directories that should be included in the TypeScript compilation process.

#### Inputs
N/A - This is a configuration property, not a function.

#### Outputs
N/A - This is a configuration property, not a function.

#### Internal Logic
- `"src"`: This indicates that all files within the `src` directory should be included for compilation.
