---
title: "apiTypes.ts"
---

## High-level description
This code defines TypeScript interfaces for various data structures used in the Trieve dashboard frontend, including user information, organizations, datasets, API keys, and Stripe billing information. It also includes several utility functions for type checking and enum conversions.

## Table of contents
- Interfaces
    - `SlimUser`
    - `UserOrganization`
    - `Organization`
    - `OrganizationAndSubAndPlan`
    - `StripeSubscription`
    - `StripePlan`
    - `OrganizationUsageCount`
    - `Dataset`
    - `DatasetUsageCount`
    - `DatasetAndUsage`
    - `ServerEnvsConfiguration`
    - `ClientEnvsConfiguration`
    - `DefaultError`
    - `ComboboxItem`
    - `ComboboxSection`
    - `SetUserApiKeyResponse`
    - `EventDTO`
    - `Event`
    - `Invitation`
    - `StripeInvoice`
    - `CreateSetupCheckoutSessionResPayload`
- Enums
    - `UserRole`
    - `ApiKeyRole`
- Functions
    - `isComboboxValues`
    - `fromI32ToUserRole`
    - `fromUserRoleToI32`
    - `stringToUserRole`
    - `fromI32ToApiKeyRole`
    - `fromApiKeyRoleToI32`
    - `stringToApiKeyRole`
    - `isEventDTO`
    - `isEvent`
    - `isInvitation`
- Constants
    - `availableEmbeddingModels`

## References
This code references types and functions from the "shared" module, indicating a shared codebase between the frontend and potentially other parts of the application.

## Symbols

### `SlimUser`
#### Description
Represents a simplified user object.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `UserOrganization`
#### Description
Represents a user's role within an organization.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `Organization`
#### Description
Represents an organization.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `OrganizationAndSubAndPlan`
#### Description
Represents an organization's subscription and plan information.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `StripeSubscription`
#### Description
Represents a Stripe subscription.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `StripePlan`
#### Description
Represents a Stripe plan.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `OrganizationUsageCount`
#### Description
Represents an organization's resource usage.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `Dataset`
#### Description
Represents a dataset.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `DatasetUsageCount`
#### Description
Represents a dataset's usage.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `DatasetAndUsage`
#### Description
Combines dataset information and usage data.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `ServerEnvsConfiguration`
#### Description
Represents server-side environment configuration for a dataset.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `ClientEnvsConfiguration`
#### Description
Represents client-side environment configuration for a dataset.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `DefaultError`
#### Description
Represents a default error object.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `ComboboxItem`
#### Description
Represents an item in a combobox.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `ComboboxSection`
#### Description
Represents a section in a combobox, containing multiple items.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `SetUserApiKeyResponse`
#### Description
Represents the response from setting a user's API key.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `isComboboxValues`
#### Description
Checks if a given value is an array of `ComboboxSection`.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| values | unknown | The value to check. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| isComboboxValues | boolean | True if the value is an array of `ComboboxSection`, false otherwise. |

#### Internal Logic
Iterates through the provided value and checks if each element is a `ComboboxSection` object with the required properties.

### `UserRole`
#### Description
Enum representing user roles within an organization.

#### Inputs
N/A - This is an enum, not a function.

#### Outputs
N/A - This is an enum, not a function.

#### Internal Logic
N/A - This is an enum, not a function.

### `fromI32ToUserRole`
#### Description
Converts an integer representation of a user role to its corresponding `UserRole` enum value.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| role | number | The integer representation of the user role. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| fromI32ToUserRole | UserRole | The corresponding `UserRole` enum value. |

#### Internal Logic
Uses a switch statement to map integer values to `UserRole` enum values.

### `fromUserRoleToI32`
#### Description
Converts a `UserRole` enum value to its corresponding integer representation.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| role | UserRole | The `UserRole` enum value to convert. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| fromUserRoleToI32 | number | The corresponding integer representation of the user role. |

#### Internal Logic
Uses a switch statement to map `UserRole` enum values to integer values.

### `stringToUserRole`
#### Description
Converts a string representation of a user role to its corresponding `UserRole` enum value.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| input | string | The string representation of the user role. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| stringToUserRole | UserRole \| undefined | The corresponding `UserRole` enum value, or undefined if the input is invalid. |

#### Internal Logic
Uses a switch statement to map string values to `UserRole` enum values.

### `ApiKeyRole`
#### Description
Enum representing API key roles.

#### Inputs
N/A - This is an enum, not a function.

#### Outputs
N/A - This is an enum, not a function.

#### Internal Logic
N/A - This is an enum, not a function.

### `fromI32ToApiKeyRole`
#### Description
Converts an integer representation of an API key role to its corresponding `ApiKeyRole` enum value.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| role | number | The integer representation of the API key role. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| fromI32ToApiKeyRole | ApiKeyRole | The corresponding `ApiKeyRole` enum value. |

#### Internal Logic
Uses a switch statement to map integer values to `ApiKeyRole` enum values.

### `fromApiKeyRoleToI32`
#### Description
Converts an `ApiKeyRole` enum value to its corresponding integer representation.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| role | ApiKeyRole | The `ApiKeyRole` enum value to convert. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| fromApiKeyRoleToI32 | number | The corresponding integer representation of the API key role. |

#### Internal Logic
Uses a switch statement to map `ApiKeyRole` enum values to integer values.

### `stringToApiKeyRole`
#### Description
Converts a string representation of an API key role to its corresponding `ApiKeyRole` enum value.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| input | string | The string representation of the API key role. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| stringToApiKeyRole | ApiKeyRole \| undefined | The corresponding `ApiKeyRole` enum value, or undefined if the input is invalid. |

#### Internal Logic
Uses a switch statement to map string values to `ApiKeyRole` enum values.

### `ApiKeyRespBody`
#### Description
Represents the response body for API key requests.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `availableEmbeddingModels`
#### Description
A constant array of objects representing available embedding models.

#### Inputs
N/A - This is a constant, not a function.

#### Outputs
N/A - This is a constant, not a function.

#### Internal Logic
N/A - This is a constant, not a function.

### `EventDTO`
#### Description
Represents a data transfer object for events.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `isEventDTO`
#### Description
Checks if a given value is an `EventDTO` object.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| data | unknown | The value to check. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| isEventDTO | boolean | True if the value is an `EventDTO` object, false otherwise. |

#### Internal Logic
Checks if the provided value is an object with the required properties and types for an `EventDTO`.

### `Event`
#### Description
Represents an event.

#### Inputs
N/A - This is an interface, not a function.

#### Outputs
N/A - This is an interface, not a function.

#### Internal Logic
N/A - This is an interface, not a function.

### `isEvent`
#### Description
Checks if a given value is an `Event` object.

#### Inputs
| Name | Type | Description |
|: