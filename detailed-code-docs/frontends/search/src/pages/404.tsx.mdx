---
title: "404.tsx"
---

## High-level description
This code defines a React component called `NotFound` that displays a 404 Not Found page within the context of a search application. It provides a simple message indicating the page wasn't found and a link to return to the home page.

## Table of contents
- `NotFound` component

## Symbols

### Symbol Name
`NotFound`

#### Description
This React functional component renders a simple "404 Not Found" page with a link to return to the home page. It utilizes the `SearchLayout` component for basic layout structure and the `Footer` component for the page footer.

#### Inputs
This component does not accept any props.

#### Outputs
- **JSX.Element:** Renders the 404 Not Found page content.

#### Internal Logic
1. **Structure:** The component uses `SearchLayout` to provide the basic layout, including a navbar and footer.
2. **Content:** It displays a "404 Not Found" message and a link to the home page ("/").
3. **Styling:** The component uses Tailwind CSS classes for styling, such as `flex`, `flex-col`, `items-center`, `text-3xl`, `text-turquoise-500`, and `underline`.

## References
- `SearchLayout`: A layout component presumably providing the overall structure for search-related pages.
- `Footer`: A component rendering the footer of the application.

## Dependencies
This code snippet does not explicitly list external library dependencies. However, it is likely that the following libraries are used based on the code and related files:

| Dependency | Purpose |
|:-----------|:--------|
| solid-js | The core library for building the user interface. |
| Tailwind CSS | A utility-first CSS framework for styling the components. |

