---
title: "FilterModal.tsx"
---

```diff
--- a/frontends/search/src/components/FilterModal.tsx
+++ b/frontends/search/src/components/FilterModal.tsx
@@ -1,6 +1,7 @@
 /* eslint-disable @typescript-eslint/no-unsafe-call */
 /* eslint-disable @typescript-eslint/no-unsafe-member-access */
 import { Show, For, createSignal, createEffect } from "solid-js";
+import { MetadataFilterField } from "../hooks/useSearch";
 
 export interface Filter {
   field: string;
@@ -149,46 +150,33 @@
           onChange={(e) =&gt; {
             setTempFilterField(e.currentTarget.value);
           }}
-          value={
-            tempFilterField().startsWith("metadata")
-              ? "metadata"
-              : tempFilterField()
-          }
+          value={tempFilterField()}
         &gt;
           &lt;For
-            each={[
-              "tag_set",
-              "link",
-              "time_stamp",
-              "location",
-              "metadata",
-              "num_value",
-            ]}
+            each={["tag_set", "link", "time_stamp", "location", "num_value"]}
           &gt;
             {(filter_field) =&gt; {
               return (
-                &lt;option
-                  classList={{
-                    "flex w-full items-center justify-between rounded p-1":
-                      true,
-                    "bg-neutral-300 dark:bg-neutral-900":
-                      tempFilterField().startsWith(filter_field),
-                  }}
-                &gt;
+                &lt;option value={filter_field}&gt;{filter_field}&lt;/option&gt;
+              );
+            }}
+          &lt;/For&gt;
+          &lt;optgroup label="metadata"&gt;
+            &lt;For each={Object.values(MetadataFilterField)}&gt;
+              {(filter_field) =&gt; {
+                return (
+                  &lt;option
+                    classList={{
+                      "flex w-full items-center justify-between rounded p-1":
+                        true,
+                      "bg-neutral-300 dark:bg-neutral-900":
+                        tempFilterField().startsWith(filter_field),
+                    }}
+                    value={filter_field}
+                  &gt;
                   {filter_field}
-                &lt;/option&gt;
-              );
-            }}
-          &lt;/For&gt;
-        &lt;/select&gt;
-        &lt;Show when={tempFilterField().startsWith("metadata")}&gt;
-          &lt;div&gt;
-            &lt;span class="p-2"&gt;.&lt;/span&gt;
-            &lt;input
-              type="text"
-              placeholder="field"
-              class="rounded-md border border-neutral-400 bg-neutral-100 p-1 dark:border-neutral-900 dark:bg-neutral-800"
-              onChange={(e) =&gt; {
-                setTempFilterField("metadata." + e.currentTarget.value);
-              }}
-              value={tempFilterField().replace("metadata", "").replace(".", "")}
-            /&gt;
-          &lt;/div&gt;
-        &lt;/Show&gt;
+                  &lt;/option&gt;
+                );
+              }}
+            &lt;/For&gt;
+          &lt;/optgroup&gt;
+        &lt;/select&gt;
       &lt;/div&gt;
       &lt;div class="w-full"&gt;
         &lt;label aria-label="Change Filter Mode"&gt;

```