---
title: "preview-head.html"
---

## High-level description

The `preview-head.html` file in the `.storybook` directory is used to configure the Storybook environment for a frontend application. It includes hidden HTML elements that store configuration data for a tool or application named "marimo." This configuration data includes settings for completion, autosave, keymap presets, and a server token.

## Code Structure

The file consists of several HTML elements, each with specific attributes that store configuration data. These elements are hidden, indicating that they are not meant to be displayed in the UI but are used for configuration purposes.

## Symbols

### `&lt;marimo-filename&gt;`
#### Description
This element stores the filename associated with the marimo configuration. It is hidden and not displayed in the UI.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| hidden | boolean | Indicates that the element is not visible in the UI. |

#### Outputs
None

### `&lt;marimo-mode&gt;`
#### Description
This element specifies the mode in which the marimo tool is operating. It is hidden and not displayed in the UI.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| data-mode | string | The mode of operation for marimo. |
| hidden | boolean | Indicates that the element is not visible in the UI. |

#### Outputs
None

### `&lt;marimo-version&gt;`
#### Description
This element indicates the version of the marimo tool being used. It is hidden and not displayed in the UI.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| data-mode | string | The version of the marimo tool. |
| hidden | boolean | Indicates that the element is not visible in the UI. |

#### Outputs
None

### `&lt;marimo-user-config&gt;`
#### Description
This element contains user-specific configuration settings for the marimo tool. It is hidden and not displayed in the UI.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| data-config | JSON string | User configuration settings for marimo. |
| hidden | boolean | Indicates that the element is not visible in the UI. |

#### Outputs
None

#### Internal Logic
The `data-config` attribute contains a JSON string with various configuration options such as completion settings, autosave preferences, keymap presets, and experimental features.

### `&lt;marimo-app-config&gt;`
#### Description
This element holds application-specific configuration settings for the marimo tool. It is hidden and not displayed in the UI.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| data-config | JSON string | Application configuration settings for marimo. |
| hidden | boolean | Indicates that the element is not visible in the UI. |

#### Outputs
None

### `&lt;marimo-server-token&gt;`
#### Description
This element stores a server token used by the marimo tool. It is hidden and not displayed in the UI.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| data-token | string | A server token for authentication or identification. |
| hidden | boolean | Indicates that the element is not visible in the UI. |

#### Outputs
None

## Dependencies

There are no external libraries or modules directly referenced in this HTML file. However, it is likely that the marimo tool or application that uses these configurations is dependent on other parts of the codebase or external libraries.

## Configuration

The file defines several configuration options for the marimo tool:

| Option | Type | Default | Description |
|:-------|:-----|:--------|:------------|
| completion.activate_on_typing | boolean | false | Determines if completion is activated on typing. |
| completion.copilot | boolean | true | Enables or disables the copilot feature. |
| save.autosave | string | "off" | Sets the autosave feature on or off. |
| save.autosave_delay | number | 0 | Delay in milliseconds for autosave. |
| keymap.preset | string | "default" | Preset for key mappings. |
| experimental | object | {} | Placeholder for experimental features. |

## Error Handling

There is no explicit error handling in this HTML file, as it primarily serves as a configuration file.

## Logging

There are no logging mechanisms implemented in this HTML file.

## API/Interface Reference

This file does not define an API or public interface. It is used for configuration purposes within the Storybook environment.

## TODOs

There are no TODOs or notes left in the code.