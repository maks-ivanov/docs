---
title: "app-chrome.css"
---

## High-level description
The CSS file `app-chrome.css` defines styles for resize handles used in a user interface component, likely part of a web application. It specifies the appearance and behavior of these handles, including their size, transition effects, and hover states.

## Code Structure
The CSS rules are organized to target different states and orientations of resize handles. The styles are applied to elements with classes `.resize-handle` and `.resize-handle-collapsed`, with additional styles for horizontal and vertical orientations.

## Symbols

### `.resize-handle, .resize-handle-collapsed`
#### Description
These classes define the base styles for resize handles, including transition effects and outline properties.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These are CSS classes applied to HTML elements. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These classes affect the visual styling of HTML elements. |

#### Internal Logic
- The `transition` property is set to create a smooth change in the `background-color` over 250 milliseconds.
- The `outline` is set to `none` to remove any default outline styling.

### `.resize-handle.horizontal, .resize-handle-collapsed.horizontal`
#### Description
These classes specify the height for horizontally oriented resize handles.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These are CSS classes applied to HTML elements. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These classes affect the visual styling of HTML elements. |

#### Internal Logic
- The `height` is set to `4px` for horizontal resize handles.

### `.resize-handle.vertical, .resize-handle-collapsed.vertical`
#### Description
These classes specify the width for vertically oriented resize handles.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These are CSS classes applied to HTML elements. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These classes affect the visual styling of HTML elements. |

#### Internal Logic
- The `width` is set to `4px` for vertical resize handles.

### `.resize-handle:hover, .resize-handle[data-resize-handle-active], .resize-handle-collapsed:hover, .resize-handle-collapsed[data-resize-handle-active]`
#### Description
These classes define the hover and active state styles for resize handles, changing the background color.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These are CSS classes applied to HTML elements. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| N/A  | N/A  | These classes affect the visual styling of HTML elements. |

#### Internal Logic
- The `background-color` is set to `var(--slate-11)` when the handle is hovered over or active, providing visual feedback to the user.

## Dependencies
The CSS file uses a CSS variable `--slate-11` for the background color, which is likely defined elsewhere in the application's stylesheet.

## Side Effects
The styles defined in this file will affect any HTML elements that have the specified classes, altering their appearance based on the defined rules.

## Performance Considerations
The use of CSS transitions can impact performance if applied to a large number of elements or if the transition properties are complex. However, the transition defined here is simple and should not significantly affect performance.