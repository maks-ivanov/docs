---
title: "switchable-multi-select.stories.tsx"
---

## High-level description

The target file `switchable-multi-select.stories.tsx` is a Storybook story for the `SwitchableMultiSelect` component. It demonstrates how the component can be used within a Storybook environment, providing a UI for selecting multiple options from a list or entering them via a textarea. The story uses a `TooltipProvider` to wrap the component, enabling tooltips within the UI.

## Code Structure

- **Meta**: Defines metadata for the Storybook story, including the title and component being documented.
- **Primary**: A story object that renders the `SwitchableMultiSelect` component with a set of predefined options and a tooltip.

## References

- **SwitchableMultiSelect**: The main component being demonstrated, imported from `@/components/forms/switchable-multi-select`.
- **TooltipProvider**: A component from `@/components/ui/tooltip` that provides context for tooltips within the story.

## Symbols

### `meta`
#### Description
The `meta` object contains metadata for the Storybook story, specifying the title and the component to be used in the story.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| title | string | The title of the story in Storybook. |
| component | React.ComponentType | The component to be rendered in the story. |
| args | object | Default arguments for the story. |

#### Outputs
None

### `Primary`
#### Description
The `Primary` story renders the `SwitchableMultiSelect` component with a set of options and manages the selected values using React state.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| args | object | Arguments passed to the component. |
| ctx | object | Contextual information for the story. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| JSX.Element | React Element | The rendered component wrapped in a `TooltipProvider`. |

#### Internal Logic
- Initializes a state variable `value` to manage the selected options.
- Renders the `SwitchableMultiSelect` component with a list of fruit options.
- Uses the `TooltipProvider` to enable tooltips within the component.
- Updates the state when the selection changes.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `@storybook/react` | Provides types and utilities for creating Storybook stories. |
| `react` | Used for managing component state and rendering. |
| `@/components/forms/switchable-multi-select` | The component being demonstrated in the story. |
| `@/components/ui/tooltip` | Provides tooltip functionality for the story. |

## Error Handling

The code does not implement specific error handling mechanisms beyond basic React component usage.

## Logging

The code does not implement any logging mechanisms.

## API/Interface Reference

The code does not expose an API or public interface beyond the Storybook story.

## TODOs

There are no TODOs or notes left in the code.