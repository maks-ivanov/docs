---
title: "table.css"
---

## High-level description

The `table.css` file is a CSS stylesheet that defines styles for tables within a web application, specifically targeting tables with the classes `.markdown` and `.dataframe`. It sets properties such as display, width, overflow, background colors for alternating rows, hover effects, text alignment, padding, and border styles to ensure consistent and visually appealing table presentations.

## Code Structure

The CSS rules in this file are organized to apply styles to tables with specific classes (`.markdown` and `.dataframe`). These styles are applied to various table elements such as `thead`, `tbody`, `tr`, `th`, and `td`. The file uses CSS variables and utility classes for styling, which are likely defined elsewhere in the codebase.

## Symbols

### `.markdown table, table.dataframe`
#### Description
This rule applies to tables with the classes `.markdown` and `.dataframe`. It ensures that these tables are displayed as block elements, have a maximum width of 100%, and can be scrolled horizontally if their content overflows.

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| display | CSS property | Sets the table to be a block element. |
| max-width | CSS property | Limits the table's width to 100% of its container. |
| overflow | CSS property | Allows horizontal scrolling if content overflows. |

### `.markdown table thead, table.dataframe thead`
#### Description
This rule styles the table header (`thead`) for both `.markdown` and `.dataframe` tables, aligning the content vertically at the bottom and applying a border style using a utility class.

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| vertical-align | CSS property | Aligns the header content to the bottom. |
| border | CSS property | Applies a border style using the `@apply border-input` utility. |

### `.markdown table tbody tr:nth-child(odd), table.dataframe tbody tr:nth-child(odd)`
#### Description
This rule applies a background color to odd-numbered rows in the table body (`tbody`) for both `.markdown` and `.dataframe` tables, using a CSS variable for the color.

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| background | CSS property | Sets the background color using `var(--lime-2)`. |

### `.markdown table tbody tr:nth-child(even), table.dataframe tbody tr:nth-child(even)`
#### Description
This rule applies a background style to even-numbered rows in the table body (`tbody`) for both `.markdown` and `.dataframe` tables, using a utility class.

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| background | CSS property | Applies a background style using `@apply bg-card`. |

### `.markdown table tbody tr:hover, table.dataframe tbody tr:hover`
#### Description
This rule changes the background color of a table row when hovered over, using a CSS variable for the color.

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| background | CSS property | Sets the hover background color using `var(--yellow-4)`. |

### `.markdown table tr, .markdown table th, .markdown table td, table.dataframe tr, table.dataframe th, table.dataframe td`
#### Description
This rule sets the text alignment, vertical alignment, padding, line height, and border style for table rows, headers, and cells in both `.markdown` and `.dataframe` tables.

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| text-align | CSS property | Aligns text to the right. |
| vertical-align | CSS property | Aligns content vertically in the middle. |
| padding-left | CSS property | Sets left padding to 0.5rem. |
| padding-right | CSS property | Sets right padding to 0.5rem. |
| line-height | CSS property | Sets line height to normal. |
| border | CSS property | Removes borders by setting them to none. |

## References

The file references CSS variables such as `--lime-2` and `--yellow-4`, and utility classes like `@apply border-input` and `@apply bg-card`, which are likely defined in other CSS files imported in the project.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `@apply` | Utilizes utility classes for styling, likely from a CSS framework like Tailwind CSS. |

## Configuration

No specific configuration options are defined in this CSS file.

## Error Handling

There is no error handling in CSS files as they are purely for styling purposes.

## Logging

There is no logging in CSS files as they are purely for styling purposes.

## API/Interface Reference

This CSS file does not define an API or public interface. It is used for styling purposes only.