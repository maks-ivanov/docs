---
title: "intent.ts"
---

## High-level description

The target file defines a TypeScript type `Intent` and a Zod schema `zodIntent` for validating and defaulting values of this type. The `Intent` type represents different states or categories such as "neutral", "success", "warn", "danger", "info", and "alert", with a note that "alert" is deprecated in favor of "danger". The Zod schema is used to enforce these values and provide a default value of "neutral".

## Code Structure

The main symbols in the code are the `Intent` type and the `zodIntent` Zod schema. The `Intent` type is a TypeScript union type that lists possible string values. The `zodIntent` schema uses Zod's `enum` method to create a validation schema for the `Intent` type, ensuring that only the specified values are allowed and setting a default value.

## References

- The `Intent` type is used in various components and plugins, such as `CalloutOutput` and `ButtonPlugin`, to specify the kind of intent or state they represent.
- The `zodIntent` schema is used in plugins like `ButtonPlugin` and `CalloutPlugin` to validate the `kind` property of their data.

## Symbols

### `Intent`
#### Description
`Intent` is a TypeScript union type that defines a set of string literals representing different states or categories. It includes "neutral", "success", "warn", "danger", "info", and "alert", with a note that "alert" is deprecated.

#### Inputs
N/A

#### Outputs
N/A

### `zodIntent`
#### Description
`zodIntent` is a Zod schema that validates values against the `Intent` type. It ensures that only the specified string literals are allowed and provides a default value of "neutral".

#### Inputs
N/A

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| output | Zod schema | A schema that validates and defaults values of the `Intent` type. |

#### Internal Logic
- Uses Zod's `enum` method to define a schema that accepts only the specified string literals.
- Sets a default value of "neutral" using Zod's `default` method.

## Side Effects
None.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `zod` | Used to create a schema for validating and defaulting values of the `Intent` type. |

## Error Handling
The Zod schema inherently handles errors by validating input against the specified enum values. If a value outside the allowed set is provided, Zod will throw a validation error.

## Logging
None.

## TODOs
- The code includes a comment indicating that the "alert" intent is deprecated and should be replaced with "danger". This suggests a future task to remove "alert" from the codebase once it is no longer in use.