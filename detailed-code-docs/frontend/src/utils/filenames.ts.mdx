---
title: "filenames.ts"
---

## High-level description

The `filenames.ts` file provides utility functions for manipulating filenames, specifically for changing file extensions and removing extensions. It includes methods to convert filenames to specific formats like Markdown, HTML, PNG, and Python, as well as a method to strip the extension from a filename.

## Code Structure

The main symbol in this file is the `Filenames` object, which contains several methods for filename manipulation. Each method is designed to either change the file extension to a specific type or remove the extension entirely. The `replace` method is a helper function used by other methods to change the file extension.

## Symbols

### `Filenames`
#### Description
The `Filenames` object provides utility methods for converting and manipulating filenames. It includes methods to change the file extension to Markdown, HTML, PNG, or Python, and a method to remove the file extension.

#### Methods

- **`toMarkdown`**
  - **Description**: Converts the file extension of a given filename to `.md`.
  - **Inputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The original filename.       |
  - **Outputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The filename with `.md` extension. |

- **`toHTML`**
  - **Description**: Converts the file extension of a given filename to `.html`.
  - **Inputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The original filename.       |
  - **Outputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The filename with `.html` extension. |

- **`toPNG`**
  - **Description**: Converts the file extension of a given filename to `.png`.
  - **Inputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The original filename.       |
  - **Outputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The filename with `.png` extension. |

- **`toPY`**
  - **Description**: Converts the file extension of a given filename to `.py`.
  - **Inputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The original filename.       |
  - **Outputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The filename with `.py` extension. |

- **`withoutExtension`**
  - **Description**: Removes the file extension from a given filename.
  - **Inputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The original filename.       |
  - **Outputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The filename without extension. |

- **`replace`**
  - **Description**: Replaces the file extension of a given filename with a specified extension.
  - **Inputs**:
    | Name      | Type   | Description                  |
    |:----------|:-------|:-----------------------------|
    | filename  | string | The original filename.       |
    | extension | string | The new extension to apply.  |
  - **Outputs**:
    | Name     | Type   | Description                  |
    |:---------|:-------|:-----------------------------|
    | filename | string | The filename with new extension. |

#### Internal Logic
- The `replace` method checks if the filename already ends with the desired extension. If it does, it returns the filename unchanged. Otherwise, it removes the current extension using `withoutExtension` and appends the new extension.

## References

- The `Filenames` object is used in various parts of the codebase, such as in `useNotebookActions` and `downloadAsHTML`, to handle filename conversions when downloading or exporting files.

## Dependencies

No external libraries are directly used in this file, but the `Filenames` object is imported and utilized in other parts of the codebase, as seen in the related snippets.

## Error Handling

The code does not include explicit error handling mechanisms. It assumes that the input filenames are valid strings and that the operations (like string manipulation) will succeed without errors.

## Logging

There are no logging mechanisms implemented in this code.

## TODOs

There are no TODOs or notes left in the code.