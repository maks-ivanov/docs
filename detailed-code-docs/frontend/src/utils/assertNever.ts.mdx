---
title: "assertNever.ts"
---

## High-level description

The `assertNever.ts` file provides utility functions for type-safe exhaustiveness checks in TypeScript. It includes two functions, `assertNever` and `logNever`, which are used to handle unexpected cases in discriminated unions. The `assertNever` function throws an error when an unexpected case is encountered, while `logNever` logs a warning message using a logger.

## Code Structure

The file contains two main functions: `assertNever` and `logNever`. Both functions are designed to handle unexpected cases in TypeScript's discriminated unions, but they do so in different ways. The `assertNever` function throws an error, while the `logNever` function logs a warning message using the `Logger` module.

## References

- The `Logger` module is imported and used in the `logNever` function to log warning messages.

## Symbols

### `assertNever`
#### Description
The `assertNever` function is used to ensure exhaustiveness in switch-case statements for discriminated unions. It throws an error if an unexpected case is encountered, indicating that the code has reached an invalid state.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| x | never | The value that should never occur. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| - | never | This function never returns as it always throws an error. |

#### Internal Logic
The function throws an error with a message that includes the unexpected object, `x`.

### `logNever`
#### Description
The `logNever` function is similar to `assertNever`, but instead of throwing an error, it logs a warning message. This is useful for exhaustiveness checks where logging the unexpected case is sufficient.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| x | never | The value that should never occur. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| - | void | This function does not return any value. |

#### Internal Logic
The function uses the `Logger` module to log a warning message that includes the unexpected object, `x`.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `Logger` | Used to log warning messages in the `logNever` function. |

## Side Effects

- The `logNever` function logs a warning message to the console using the `Logger` module.

## Logging

The `logNever` function implements logging by using the `Logger` module to log a warning message when an unexpected case is encountered. This provides a non-intrusive way to handle unexpected cases without throwing an error.