---
title: "mimetypes.py"
---

## High-level description

The `mimetypes.py` file defines a set of known MIME types using Python's `Literal` type from the `typing` module. This approach helps to avoid typos and ensures that only predefined MIME types are used throughout the codebase. These MIME types are likely used in various parts of the application to specify the format of data being processed or transmitted.

## Symbols

### `KnownMimeType`
#### Description
`KnownMimeType` is a type alias for a `Literal` type that enumerates a set of predefined MIME types. This type is used to ensure that only these specific MIME types are used in the code, reducing the risk of typos and errors related to MIME type strings.

#### Inputs
This is a type definition and does not take inputs directly. It is used as a type hint in other parts of the code.

#### Outputs
This is a type definition and does not produce outputs directly. It is used to enforce type safety in other parts of the code.

## References

- The `KnownMimeType` is referenced in multiple files across the codebase, such as `marimo/_ast/app.py`, `marimo/_cli/development/commands.py`, `marimo/_messaging/cell_output.py`, `marimo/_messaging/console_output_worker.py`, `marimo/_messaging/ops.py`, `marimo/_messaging/streams.py`, `marimo/_output/formatters/altair_formatters.py`, `marimo/_output/formatters/anywidget_formatters.py`, `marimo/_output/formatters/bokeh_formatters.py`, `marimo/_output/formatters/cell.py`, `marimo/_output/formatters/df_formatters.py`, `marimo/_output/formatters/holoviews_formatters.py`, `marimo/_output/formatters/ipython_formatters.py`, `marimo/_output/formatters/leafmap_formatters.py`, `marimo/_output/formatters/matplotlib_formatters.py`, `marimo/_output/formatters/pandas_formatters.py`, `marimo/_output/formatters/plotly_formatters.py`, `marimo/_output/formatters/seaborn_formatters.py`, `marimo/_output/formatters/structures.py`, `marimo/_output/formatting.py`, `marimo/_output/hypertext.py`, `marimo/_output/mime.py`, `marimo/_output/mpl.py`, `marimo/_output/utils.py`, `marimo/_plugins/stateless/status/_progress.py`, `marimo/_pyodide/streams.py`, `marimo/_runtime/params.py`, `marimo/_runtime/virtual_file.py`, `marimo/_server/export/exporter.py`, and `tests/_output/formatters/test_formatters.py`. These references indicate that the `KnownMimeType` is used extensively throughout the application to handle data formats consistently.

## Dependencies

The file does not have any external dependencies beyond the standard library's `typing` module.

## Error Handling

There is no error handling in this file as it only defines a type alias.

## Logging

There is no logging in this file.

## API/Interface Reference

This file does not define an API or interface directly. It provides a type alias used in other parts of the codebase.

## TODOs

There are no TODOs or notes in this file.