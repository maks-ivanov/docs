---
title: "style.py"
---

## High-level description

The code in `marimo/_plugins/stateless/style.py` defines a function `style` that wraps an object in a styled HTML container. This function takes an object and a dictionary of CSS styles, applies the styles to the object, and returns an HTML representation of the styled object. It is part of the Marimo library, which is designed to enhance interactive notebooks with dynamic UI elements.

## Code Structure

The main symbol in this code is the `style` function. It utilizes several other components from the Marimo library, such as `Html` from `marimo._output.hypertext`, `h` from `marimo._output.builder`, and `as_html` from `marimo._output.formatting`. These components work together to convert the input object into an HTML format and apply the specified styles.

## References

- `Html`: A class from `marimo._output.hypertext` that represents HTML content.
- `h.div`: A method from `_HTMLBuilder` in `marimo._output.builder` used to create a `&lt;div&gt;` element.
- `as_html`: A function from `marimo._output.formatting` that converts an object to an HTML representation.

## Symbols

### `style`
#### Description
The `style` function wraps an object in a styled HTML container. It takes an object and a dictionary of CSS styles, converts the object to HTML, and applies the styles to it.

#### Inputs
| Name   | Type         | Description                          |
|:-------|:-------------|:-------------------------------------|
| item   | object       | The object to render as HTML.        |
| styles | dict[str, Any] | A dictionary of CSS styles keyed by property name. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| Html | Html | An HTML object with the applied styles. |

#### Internal Logic
1. The function constructs a CSS style string by joining the key-value pairs from the `styles` dictionary.
2. It converts the `item` to an HTML string using the `as_html` function.
3. It creates a `&lt;div&gt;` element using `h.div`, setting the `children` to the HTML text of the item and the `style` attribute to the constructed style string.
4. It returns an `Html` object containing the styled `&lt;div&gt;`.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `marimo._output.builder` | Provides the `h` object for building HTML elements. |
| `marimo._output.formatting` | Provides the `as_html` function to convert objects to HTML. |
| `marimo._output.hypertext` | Provides the `Html` class for representing HTML content. |
| `marimo._output.rich_help` | Provides the `mddoc` decorator for documentation. |

## Error Handling

The code does not explicitly handle errors. It assumes that the inputs are valid and that the `as_html` function can successfully convert the `item` to HTML.

## Logging

There is no logging implemented in this code.

## TODOs

There are no TODOs or notes left in the code.