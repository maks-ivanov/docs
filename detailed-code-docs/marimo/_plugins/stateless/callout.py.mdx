---
title: "callout.py"
---

## High-level description

The `callout.py` file defines a function `callout` that generates an HTML representation of a callout box, which is a UI component used to highlight or emphasize content. The function allows customization of the callout's appearance through different styles, such as "neutral", "warn", "success", "info", and "danger".

## Code Structure

The main symbol in this file is the `callout` function. It utilizes several imported modules and functions to achieve its functionality:
- `as_html` from `marimo._output.formatting` is used to convert the input value into an HTML format.
- `Html` from `marimo._output.hypertext` is used to wrap the final HTML string.
- `build_stateless_plugin` from `marimo._plugins.core.web_component` is used to construct the HTML for the callout component.

## Symbols

### `callout`
#### Description
The `callout` function is designed to create a styled HTML callout box that can be used to emphasize or highlight content in a web application. It takes a value to display and a style kind that determines the visual appearance of the callout.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| value | object | The content to be rendered inside the callout box. |
| kind | Literal["neutral", "warn", "success", "info", "danger"] | The style of the callout, which affects its visual appearance. Defaults to "neutral". |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| output | Html | An HTML object representing the callout box with the specified content and style. |

#### Internal Logic
1. The function first converts the `value` into an HTML string using the `as_html` function.
2. It then calls `build_stateless_plugin` to construct the HTML for the callout component. This function takes the component name "marimo-callout-output" and a dictionary of arguments, including the HTML text and the kind of callout.
3. The resulting HTML string is wrapped in an `Html` object, which is then returned.

## References

- **`as_html`**: This function is used to convert the input value into an HTML format. It is defined in `marimo._output.formatting`.
- **`Html`**: This class is used to wrap the final HTML string. It is defined in `marimo._output.hypertext`.
- **`build_stateless_plugin`**: This function is used to construct the HTML for the callout component. It is defined in `marimo._plugins.core.web_component`.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `marimo._output.formatting` | Provides the `as_html` function to convert objects to HTML. |
| `marimo._output.hypertext` | Provides the `Html` class to encapsulate HTML content. |
| `marimo._plugins.core.web_component` | Provides the `build_stateless_plugin` function to create HTML for stateless components. |

## Logging

There is no explicit logging implemented in this file.

## Error Handling

The function does not implement specific error handling mechanisms beyond basic exception raising. It assumes that the inputs are valid and that the imported functions handle any necessary validation or error management.