---
title: "json_output.py"
---

## High-level description

The `json_output.py` file defines a function `json_output` that generates an HTML representation of JSON-serializable data. This function is part of a plugin system for the Marimo framework, which is used to create web components. The function leverages the `Html` class to wrap the generated HTML string, allowing it to be rendered in a web application.

## Code Structure

The main function in this file is `json_output`, which utilizes the `Html` class from the `marimo._output.hypertext` module and the `build_stateless_plugin` function from the `marimo._plugins.core.web_component` module. The `json_output` function is designed to create a stateless web component that displays JSON data.

## Symbols

### `json_output`
#### Description
The `json_output` function creates an HTML element that displays JSON-serializable data. It optionally includes a text label. The function returns an `Html` object, which is a wrapper around the generated HTML string, allowing it to be rendered in a web application.

#### Inputs
| Name      | Type       | Description                          |
|:----------|:-----------|:-------------------------------------|
| json_data | JSONType   | JSON-serializable data to display.   |
| name      | Optional[str] | An optional text label for the JSON data. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| Html | Html | An `Html` object containing the HTML representation of the JSON data. |

#### Internal Logic
1. The function calls `build_stateless_plugin` with the component name `"marimo-json-output"`.
2. It constructs a dictionary of arguments, including the JSON data and an optional name.
3. The `build_stateless_plugin` function generates the HTML string for the component.
4. The resulting HTML string is wrapped in an `Html` object and returned.

## References

- **`Html` class**: Used to wrap the generated HTML string. It is defined in the `marimo._output.hypertext` module.
- **`build_stateless_plugin` function**: Used to generate the HTML for the stateless web component. It is defined in the `marimo._plugins.core.web_component` module.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `marimo._output.hypertext.Html` | Wraps the generated HTML string for rendering. |
| `marimo._plugins.core.web_component.build_stateless_plugin` | Generates the HTML for the stateless web component. |

## Error Handling

The function does not implement specific error handling mechanisms. It assumes that the inputs are valid JSON-serializable data and an optional string for the name.

## Logging

The function does not implement any logging mechanisms.

## TODOs

There are no TODOs or notes left in the code.