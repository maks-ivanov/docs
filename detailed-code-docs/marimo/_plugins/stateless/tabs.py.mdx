---
title: "tabs.py"
---

## High-level description

The target file `marimo/_plugins/stateless/tabs.py` defines a deprecated function `tabs` that is used to create a tabbed UI element. This function takes a dictionary of tab names and their corresponding content, and returns an `Html` object representing the tabbed interface. The function is marked as deprecated, advising users to use `mo.ui.tabs` instead.

## Code Structure

The main symbol in this file is the `tabs` function. It utilizes the `Html` class from `marimo._output.hypertext` to return an HTML representation of the tabbed UI. The function is decorated with `@mddoc` for documentation purposes and `@deprecated` to issue a deprecation warning. The actual implementation of the tabbed UI is delegated to `tabs_impl` from `marimo._plugins.ui._impl.tabs`.

## References

- `Html`: A class from `marimo._output.hypertext` used to wrap HTML text for rendering.
- `tabs_impl`: The actual implementation of the tabbed UI, imported from `marimo._plugins.ui._impl.tabs`.
- `deprecated`: A decorator from `marimo._utils.deprecated` used to mark the function as deprecated.

## Symbols

### `tabs`
#### Description
The `tabs` function is a deprecated utility for creating a tabbed UI element. It takes a dictionary mapping tab names to their content and returns an `Html` object that represents the tabbed interface. The function is intended to be replaced by `mo.ui.tabs`.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| tabs | dict[str, object] | A dictionary where keys are tab names and values are the content for each tab. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| output | Html | An `Html` object representing the tabbed UI. |

#### Internal Logic
1. The function is decorated with `@deprecated`, which issues a warning when the function is called.
2. It uses the `tabs_impl` function to generate the tabbed UI.
3. The result is wrapped in an `Html` object, which is then returned.

## Side Effects
- Emits a deprecation warning when the function is called.

## Dependencies

| Dependency | Purpose |
|:-----------|:--------|
| `marimo._output.hypertext.Html` | Used to create an HTML representation of the tabbed UI. |
| `marimo._plugins.ui._impl.tabs.tabs` | Provides the actual implementation of the tabbed UI. |
| `marimo._utils.deprecated.deprecated` | Decorator to mark the function as deprecated. |

## Error Handling
The function does not implement specific error handling mechanisms beyond what is provided by the `tabs_impl` function.

## Logging
No logging mechanisms are implemented in this code.

## TODOs
There are no TODOs or additional notes in the code.