---
title: "print.py"
---

## High-level description

The target file `marimo/_cli/print.py` provides utility functions for formatting text with ANSI color codes. These functions are used to colorize text output in the terminal, specifically in green, orange, and red colors. This is useful for enhancing the readability of command-line interface (CLI) outputs by highlighting important information or errors.

## Code Structure

The file contains three functions: `green`, `orange`, and `red`. Each function takes a string as input and returns the string wrapped in ANSI escape codes that change the text color when printed in a terminal.

## Symbols

### `green`
#### Description
The `green` function formats a given text string to appear in green when printed in a terminal. This is typically used to indicate success or positive information.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| text | str | The text to be colorized in green. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| output | str | The input text wrapped in ANSI escape codes for green color. |

### `orange`
#### Description
The `orange` function formats a given text string to appear in orange (or yellow, depending on terminal support) when printed in a terminal. This is often used for warnings or informational messages.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| text | str | The text to be colorized in orange. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| output | str | The input text wrapped in ANSI escape codes for orange color. |

### `red`
#### Description
The `red` function formats a given text string to appear in red when printed in a terminal. This is typically used to indicate errors or critical information.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| text | str | The text to be colorized in red. |

#### Outputs
| Name | Type | Description |
|:-----|:-----|:------------|
| output | str | The input text wrapped in ANSI escape codes for red color. |

## References

The functions in this file are used in other parts of the codebase to provide colorized output in the CLI. For example, the `red` function is used in `marimo/_cli/cli.py` to display error messages in red, enhancing the visibility of errors in the command-line interface.

## Dependencies

This file does not have any external dependencies. It relies solely on Python's built-in string manipulation capabilities and ANSI escape codes for terminal colorization.

## Side Effects

There are no side effects from these functions other than the intended colorization of text when printed in a terminal that supports ANSI escape codes.

## Performance Considerations

The functions are simple and efficient, as they only involve string concatenation. The performance impact is negligible in the context of CLI applications.