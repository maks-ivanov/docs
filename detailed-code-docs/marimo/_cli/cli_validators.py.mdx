---
title: "cli_validators.py"
---

## High-level description

The target file `marimo/_cli/cli_validators.py` contains a function `base_url` that serves as a validator for command-line interface (CLI) inputs, specifically for validating URL paths. It ensures that the provided URL path starts with a forward slash (`/`), which is a common requirement for URL paths in web applications.

## Symbols

### `base_url`
#### Description
The `base_url` function is a validator used in a CLI context to ensure that a given URL path is correctly formatted. It checks if the input value is either `None` or an empty string, returning an empty string in such cases. If the input is a non-empty string, it verifies that the string starts with a forward slash (`/`). If not, it raises a `click.BadParameter` exception, indicating that the input is invalid.

#### Inputs
| Name  | Type         | Description                          |
|:------|:-------------|:-------------------------------------|
| ctx   | Any          | The context object (not used).       |
| param | Any          | The parameter object (not used).     |
| value | Optional[str]| The URL path to be validated.        |

#### Outputs
| Name  | Type | Description                          |
|:------|:-----|:-------------------------------------|
| value | str  | The validated URL path or an empty string if the input was `None` or empty. |

#### Internal Logic
1. The function first deletes the `ctx` and `param` inputs as they are not used in the logic.
2. It checks if the `value` is `None` or an empty string. If so, it returns an empty string.
3. If `value` is a non-empty string, it checks whether it starts with a forward slash (`/`).
4. If the string does not start with a forward slash, it raises a `click.BadParameter` exception with a message indicating the requirement.
5. If the string is valid, it returns the `value`.

## References
The function uses the `click` library, specifically the `click.BadParameter` exception, to handle invalid input cases. This indicates that the function is likely used in a CLI application built with the `click` library.

## Dependencies
| Dependency | Purpose                                      |
|:-----------|:---------------------------------------------|
| `click`    | Used for handling command-line interface input validation and error reporting. |

## Error Handling
The function raises a `click.BadParameter` exception if the input string does not start with a forward slash. This exception is part of the `click` library and is used to indicate that a parameter provided to a CLI command is invalid.

## Side Effects
There are no side effects in this function as it only performs validation and raises an exception if the validation fails. It does not modify any global state or external resources.