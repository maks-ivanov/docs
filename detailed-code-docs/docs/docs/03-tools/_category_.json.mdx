---
title: "_category_.json"
---

## High-level description

This JSON file defines the category label for the "03-tools" section in the documentation. It sets the label to "Writing GPTScripts", which suggests that this section of the documentation is focused on providing information about writing GPTScripts.

## Symbols

### JSON Object
#### Description
The file contains a single JSON object with one key-value pair. This object is used to configure the category label for a specific section of the documentation.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| label | string | The text to be used as the category label |

#### Outputs
This JSON object doesn't produce any outputs directly. It's likely used by a documentation system or static site generator to organize and display the documentation structure.

## Dependencies
This file doesn't explicitly depend on any external libraries or modules. However, it's likely part of a larger documentation system, possibly using a static site generator like Docusaurus, which would interpret and use this JSON configuration.

## Configuration
| Option | Type | Default | Description |
|:-------|:-----|:--------|:------------|
| label | string | "Writing GPTScripts" | Defines the display label for the "03-tools" category in the documentation |

This JSON file serves as a configuration for the documentation structure, specifically for the "03-tools" section. The "label" key is used to set the display name for this category in the documentation navigation or table of contents.