---
title: "schema.rs"
---

## High-level description
This Rust code defines the database schema for an application using the Diesel ORM. It specifies the structure of various tables, their columns, data types, and relationships between them. This schema likely represents data structures for managing datasets, chunks of data, user interactions, and organizational information.

## Table of contents
- `chunk_group` table definition
- `chunk_group_bookmarks` table definition
- `chunk_metadata` table definition
- `chunk_metadata_tags` table definition
- `dataset_event_counts` table definition
- `dataset_group_counts` table definition
- `dataset_tags` table definition
- `dataset_usage_counts` table definition
- `datasets` table definition
- `files` table definition
- `groups_from_files` table definition
- `invitations` table definition
- `messages` table definition
- `organization_usage_counts` table definition
- `organizations` table definition
- `stripe_invoices` table definition
- `stripe_plans` table definition
- `stripe_subscriptions` table definition
- `topics` table definition
- `user_api_key` table definition
- `user_organizations` table definition
- `users` table definition
- Table relationships defined with `diesel::joinable!`
- Allowing tables to appear in the same query with `diesel::allow_tables_to_appear_in_same_query!`

## References
This code references the `diesel` crate, indicating its use for database interactions.

## Dependencies
| Dependency | Purpose |
|:-----------|:--------|
| diesel      | Object-relational mapper (ORM) for database interactions in Rust |

## Code Structure
The code primarily consists of individual table definitions using `diesel::table!`. Each table definition outlines the table name, primary key, and columns with their respective data types. Relationships between tables are established using `diesel::joinable!`, specifying foreign key constraints. Finally, `diesel::allow_tables_to_appear_in_same_query!` enables querying multiple tables together.
