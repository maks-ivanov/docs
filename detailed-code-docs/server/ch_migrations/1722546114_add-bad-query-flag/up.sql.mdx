---
title: "up.sql"
---

## High-level description
This SQL script alters the `search_queries` table by adding a new column named `query_rating` with a default value of '0'. The column is of type String and is added only if it doesn't already exist.

## Table of contents
- ALTER TABLE statement
- ADD COLUMN clause
- Column definition

## Symbols

### ALTER TABLE search_queries
#### Description
This SQL command modifies the structure of the existing `search_queries` table.

### ADD COLUMN IF NOT EXISTS query_rating
#### Description
This clause adds a new column named `query_rating` to the `search_queries` table, but only if a column with this name doesn't already exist.

### query_rating String DEFAULT 0
#### Description
This defines the new column `query_rating` with the following characteristics:
- Data type: String
- Default value: '0'

## Future Improvements
- Consider using a more specific data type for `query_rating` if it's intended to store numeric ratings (e.g., INTEGER or FLOAT).
- If the rating system is predefined, consider using an ENUM type instead of String for better data integrity.
- Add a CHECK constraint to ensure the `query_rating` values fall within an expected range.
- Consider adding an index on this column if it will be frequently used in WHERE clauses or JOIN conditions.