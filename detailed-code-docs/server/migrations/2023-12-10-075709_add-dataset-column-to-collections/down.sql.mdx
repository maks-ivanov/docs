---
title: "down.sql"
---

## High-level description
This SQL script defines a database migration that reverts the changes introduced by a previous migration, specifically the addition of a `dataset_id` column to the `card_metadata` and `card_collection` tables.

## Table of contents
- Dropping `dataset_id` column from `card_metadata` table
- Dropping `dataset_id` column from `card_collection` table

## Symbols

### `ALTER TABLE card_metadata`
#### Description
This SQL statement modifies the existing `card_metadata` table by dropping the `dataset_id` column if it exists.

#### Inputs
N/A - This is a SQL statement, not a function.

#### Outputs
N/A - This is a SQL statement, not a function.

#### Internal Logic
- `ALTER TABLE card_metadata`: Specifies the table to be modified.
- `DROP COLUMN IF EXISTS dataset_id`: Drops the column named `dataset_id` if it exists in the table. The `IF EXISTS` clause prevents an error if the column doesn't exist.

### `ALTER TABLE card_collection`
#### Description
This SQL statement modifies the existing `card_collection` table by dropping the `dataset_id` column if it exists.

#### Inputs
N/A - This is a SQL statement, not a function.

#### Outputs
N/A - This is a SQL statement, not a function.

#### Internal Logic
- `ALTER TABLE card_collection`: Specifies the table to be modified.
- `DROP COLUMN IF EXISTS dataset_id`: Drops the column named `dataset_id` if it exists in the table. The `IF EXISTS` clause prevents an error if the column doesn't exist.
