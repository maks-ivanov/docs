---
title: "down.sql"
---

## High-level description
This SQL migration script is designed to revert changes made in the corresponding `up.sql` file. It adds a column named `tag_set` back to the `chunk_metadata` table.

## Table of contents
- ALTER TABLE statement

## Symbols

### ALTER TABLE chunk_metadata
#### Description
This SQL command modifies the structure of the `chunk_metadata` table by adding a new column.

#### Internal Logic
1. Adds a new column named `tag_set` to the `chunk_metadata` table.
2. Sets the data type of the new column to `TEXT[]` (an array of text values).
3. Specifies a default value of an empty array (`'{}'`) for the new column.

## Side Effects
- Modifies the structure of the `chunk_metadata` table in the database.
- May affect existing queries or application code that interacts with this table.

## Future Improvements
- Consider adding any necessary indexes on the `tag_set` column if it will be frequently queried.
- Ensure that any data migration scripts are in place to populate the `tag_set` column with appropriate values if needed.