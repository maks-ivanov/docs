---
title: "down.sql"
---

## High-level description
This SQL migration script is designed to revert the changes made by the corresponding `up.sql` file. It removes the `card_html` column from the `card_metadata` table in the database.

## Table of contents
- ALTER TABLE statement

## Symbols

### ALTER TABLE card_metadata
#### Description
This SQL command modifies the structure of the `card_metadata` table by removing a specific column.

#### Internal Logic
The command drops the `card_html` column from the `card_metadata` table.

## Side Effects
- Removes the `card_html` column from the `card_metadata` table
- Any data stored in the `card_html` column will be permanently deleted

## Future Improvements
- Consider adding a backup mechanism before dropping columns to prevent accidental data loss
- If the `card_html` column contained important data, consider adding a data migration step to preserve the information in another form before dropping the column