---
title: "up.sql"
---

## High-level description
This SQL migration script adds a new column called `referral_tokens` of type TEXT to the existing `invitations` table in the database.

## Table of contents
- ALTER TABLE statement

## Symbols

### ALTER TABLE invitations
#### Description
This SQL command modifies the structure of the `invitations` table by adding a new column.

#### Internal Logic
1. Identifies the `invitations` table as the target for modification.
2. Adds a new column named `referral_tokens` with the data type TEXT.

## Side Effects
- Modifies the schema of the `invitations` table.
- Existing rows in the `invitations` table will have NULL values for the new `referral_tokens` column unless a default value is specified (which is not the case in this migration).

## Future Improvements
- Consider adding a default value for the `referral_tokens` column if appropriate.
- If referral tokens are expected to have a specific format or length, consider using a more specific data type or adding constraints to ensure data integrity.
- Depending on the application's needs, an index on the `referral_tokens` column might be beneficial for query performance if it's frequently used in WHERE clauses or JOIN conditions.