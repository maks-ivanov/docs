---
title: "down.sql"
---

## High-level description
This SQL migration script is designed to revert changes made in the corresponding `up.sql` file. It adds back two columns, `side` and `normal_chat`, to the `topics` table in the database.

## Table of contents
- Add `side` column to `topics` table
- Add `normal_chat` column to `topics` table

## Symbols

### ALTER TABLE topics ADD COLUMN side VARCHAR(255);
#### Description
This SQL statement adds a column named `side` to the `topics` table. The column is defined as a VARCHAR with a maximum length of 255 characters.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| topics | table | The existing table to be altered |

#### Outputs
The `topics` table is modified to include the new `side` column.

### ALTER TABLE topics ADD COLUMN normal_chat BOOLEAN DEFAULT TRUE;
#### Description
This SQL statement adds a column named `normal_chat` to the `topics` table. The column is defined as a BOOLEAN with a default value of TRUE.

#### Inputs
| Name | Type | Description |
|:-----|:-----|:------------|
| topics | table | The existing table to be altered |

#### Outputs
The `topics` table is modified to include the new `normal_chat` column.

## Side Effects
- Modifies the structure of the `topics` table in the database.
- May affect existing queries or applications that interact with the `topics` table.

## Future Improvements
- Consider adding appropriate indexes on the new columns if they will be frequently used in queries.
- Ensure that any application code that interacts with the `topics` table is updated to handle these new columns.
- If data migration is needed, consider adding INSERT or UPDATE statements to populate the new columns with appropriate values.