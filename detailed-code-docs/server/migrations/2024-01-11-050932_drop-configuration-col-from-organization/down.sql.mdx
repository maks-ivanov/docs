---
title: "down.sql"
---

## High-level description
This SQL migration script is designed to revert changes made in the corresponding `up.sql` file. It adds a `configuration` column of type `jsonb` to the `organizations` table.

## Table of contents
- SQL ALTER TABLE statement

## Symbols

### ALTER TABLE statement
#### Description
This SQL statement modifies the structure of the `organizations` table by adding a new column named `configuration`.

#### Internal Logic
1. Targets the `organizations` table
2. Adds a new column named `configuration`
3. Sets the data type of the new column to `jsonb` (JSON Binary)
4. Specifies that the column cannot contain NULL values (NOT NULL)
5. Sets a default value for the column (which is not specified in the given script)

## Side Effects
- Modifies the structure of the `organizations` table
- May affect existing queries or applications that interact with the `organizations` table

## Future Improvements
- Specify a default value for the `configuration` column to ensure data integrity
- Consider adding a comment to explain the purpose and structure of the `configuration` data
- Evaluate if any data migration is needed to populate the new column for existing rows