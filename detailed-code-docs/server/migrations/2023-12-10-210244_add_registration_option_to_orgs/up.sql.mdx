---
title: "up.sql"
---

## High-level description
This SQL migration script adds a new boolean column named `registerable` to the `organizations` table, with a default value of `TRUE`.

## Table of contents
- ALTER TABLE statement

## Symbols

### ALTER TABLE organizations
#### Description
This SQL command modifies the structure of the `organizations` table by adding a new column.

#### Internal Logic
1. Targets the `organizations` table
2. Adds a new column named `registerable`
3. Sets the data type of the new column to `BOOLEAN`
4. Specifies a default value of `TRUE` for the new column

## Side Effects
- Modifies the schema of the `organizations` table
- Existing rows in the `organizations` table will have the `registerable` column set to `TRUE` by default

## Future Improvements
- Consider adding a migration script to populate the `registerable` column with specific values for existing organizations if needed
- Ensure that application code is updated to handle this new column when interacting with the `organizations` table