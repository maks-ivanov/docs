---
title: "up.sql"
---

## High-level description
This SQL migration script adds a new boolean column named `normal_chat` to the `topics` table with a default value of `false`.

## Table of contents
- ALTER TABLE statement

## Symbols

### ALTER TABLE statement
#### Description
This SQL statement modifies the structure of the existing `topics` table by adding a new column.

#### Internal Logic
1. Targets the `topics` table for modification.
2. Adds a new column named `normal_chat`.
3. Sets the data type of the new column to `boolean`.
4. Specifies that the column cannot contain NULL values (NOT NULL).
5. Sets a default value of `false` for the new column.

## Side Effects
- Modifies the schema of the `topics` table.
- Existing rows in the `topics` table will have the `normal_chat` column set to `false`.

## Future Improvements
- Consider adding an index on the `normal_chat` column if it will be frequently used in WHERE clauses or JOIN conditions.
- If this column is part of a feature rollout, consider adding a database trigger or application logic to manage the transition of existing topics to "normal chat" status.