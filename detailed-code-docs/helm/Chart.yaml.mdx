---
title: "Chart.yaml"
---

## High-level description
This code defines a Helm chart for deploying the "Trieve" application. It specifies dependencies on Redis, PostgreSQL, and Qdrant, allowing for flexible deployment configurations. The chart version is 0.1.0, while the application version is 1.16.0.

## Table of contents
- Metadata
- Dependencies

## Symbols
### `Chart.yaml`
#### Description
This file provides metadata and dependencies for the Trieve Helm chart. It defines the chart's version, application version, and dependencies on other charts.

#### Inputs
N/A - This is a static configuration file.

#### Outputs
N/A - This file is used as input by Helm.

#### Internal Logic
The file uses YAML format to define key-value pairs for metadata and dependencies. Dependencies are defined with conditions, allowing users to enable or disable them based on their configuration.

## Dependencies
This chart depends on the following Helm charts:

| Dependency | Purpose | Condition |
|:-----------|:--------|:----------|
| redis | Provides Redis functionality for Trieve | `config.redis.useSubchart` |
| postgresql | Provides PostgreSQL database for Trieve | `postgres.useSubchart` |
| qdrant | Provides Qdrant vector search engine for Trieve | `config.qdrant.useSubchart` |

### Configuration
The chart's dependencies can be configured using the following options:

| Option | Type | Default | Description |
|:-------|:-----|:--------|:------------|
| `config.redis.useSubchart` | boolean | N/A | Enables/disables the Redis dependency |
| `postgres.useSubchart` | boolean | N/A | Enables/disables the PostgreSQL dependency |
| `config.qdrant.useSubchart` | boolean | N/A | Enables/disables the Qdrant dependency |
