---
title: "unifom_leaf_subsampler_test.py"
---

```diff
--- a/test/simulator_tests/unifom_leaf_subsampler_test.py
+++ b/test/simulator_tests/unifom_leaf_subsampler_test.py
@@ -138,6 +138,30 @@
         ]
         self.assertEqual(set(res.edges), set(expected_edges))
 
+    def test_subsample_tree_with_unifurcations(self):
+        tree = nx.DiGraph()
+        tree.add_edges_from(
+            [
+                ("n0", "n1"),
+                ("n1", "n2"),
+                ("n2", "n3"),
+                ("n3", "n4"),
+                ("n4", "n5"),
+                ("n2", "n6"),
+                ("n0", "n7"),
+                ("n7", "n8"),
+                ("n8", "n9"),
+                ("n9", "n10"),
+            ]
+        )
+        tree = CassiopeiaTree(tree=tree)
+        uni = UniformLeafSubsampler(ratio=0.5)
+        res = uni.subsample_leaves(tree=tree)
+        # test that it executes without error
+        self.assertTrue(True)
+
+
 
 if __name__ == "__main__":
     unittest.main()

```